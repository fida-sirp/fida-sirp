import styled from 'styled-components';

const StyledButtonTransparent = styled.button`
  background-color: Transparent;
  border: none;
  outline: none;
  height: 32px;
  width: 32px;
  cursor: pointer;
`;

export default StyledButtonTransparent;
